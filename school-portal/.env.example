# ChatGPT AI Configuration
# Copy this to .env.local and fill in your actual values

# ========================================
# OpenAI API Configuration
# ========================================

# Get your API key from: https://platform.openai.com/api-keys
# Format: sk-...
OPENAI_API_KEY=sk-your-api-key-here

# Available models:
# - gpt-4 (Most capable, slower, expensive)
# - gpt-4-turbo (Fast & powerful)
# - gpt-3.5-turbo (Fast & cheap - RECOMMENDED for starting)
# - gpt-4o (Latest, very fast)
# - gpt-4o-mini (Cheapest option)
OPENAI_MODEL=gpt-3.5-turbo

# Optional: Organization ID if you have a managed account
OPENAI_ORG_ID=

# ========================================
# AI Feature Configuration
# ========================================

# Enable/Disable AI features
ENABLE_AI_REMARKS=true
ENABLE_AI_REPORTS=true
ENABLE_AI_CHAT=false

# Rate limiting (requests per minute per user)
AI_RATE_LIMIT=10

# Maximum tokens for each request
AI_MAX_TOKENS=150

# Temperature (0 = deterministic, 1 = creative)
AI_TEMPERATURE=0.7

# ========================================
# Logging & Monitoring
# ========================================

# Enable logging of AI requests (for debugging)
LOG_AI_REQUESTS=true

# ========================================
# IMPORTANT NOTES
# ========================================
# 1. NEVER commit .env.local to GitHub
# 2. NEVER share your API key with anyone
# 3. Use .env.local for local development
# 4. Use platform environment variables for production (Vercel, etc)
# 5. Add .env.local to .gitignore
