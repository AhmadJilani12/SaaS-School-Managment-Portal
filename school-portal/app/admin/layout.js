export const metadata = {
  title: 'Admin Dashboard - School Portal',
  description: 'School Portal Admin Dashboard',
};

export default function AdminLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}